#include "main.h"
/**
 * _islower - shows 1 if the character is lowercase
 * @c: a character in ASCII code
 * Return: returns 1 for lowercase and 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
	{
		return (1);
	}
	else
	{
		return (0);
	}

