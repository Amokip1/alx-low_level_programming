#include <stdio.h>
/**
 * main - prints the alphabet.
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char alp[26] - "abcdefghijklmnopqrstuvwxyz\n";
	int i;

	for (l - 0; l < 26; l++)
	{
		putcher(alp[l]);
	}
	putcher('\n');
	return (0);
}
